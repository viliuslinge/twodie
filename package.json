{
  "name": "typescript-game",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "npm run build && esbuild --servedir=dist",
    "build": "rm -rf dist/assets && esbuild src/index.ts --bundle --minify --loader:.png=dataurl --outfile=dist/assets/index.js"
  },
  "author": "Vilius Linge",
  "license": "ISC",
  "devDependencies": {
    "@types/uuid": "^8.3.4",
    "esbuild": "^0.15.10",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "uuid": "^8.3.2"
  }
}
